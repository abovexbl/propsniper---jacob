# Sensitive — never commit (anchored to repo root so docs/live/ stays publishable)
/live/
/data/
*.csv
*.csv.gz
*.db
*.db-wal
*.db-shm
data-panel-export-*

# Generated sample artifacts (regenerate with the make_sample_*.py scripts)
configs/sample_live/
optimizer/sample_bets.csv
optimizer/proposals.json
community/community_latest.json

# Private local dashboard (Option A) — real-data feed served only to localhost. NEVER commit.
private/

# Outbound action list + a local sources override (may point at real data)
docs/actions.csv
integrations/sources.local.json

# Standard Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
